---
import Header from '../components/header.astro';
import Footer from '../components/footer.astro';
import { getLogoPath, getLogoAlt, getFavicons } from '../config/site';
import { generateThemeCSS } from '../utils/theme';
import '../styles/global.css';
import '@fortawesome/fontawesome-free/css/all.min.css';
const { pageTitle } = Astro.props;

const logoPath = getLogoPath();
const logoAlt = getLogoAlt();
const themeCSS = generateThemeCSS();
const favicons = getFavicons();
---

<html lang="de">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" sizes="32x32" href={favicons.favicon32x32} />
    <link rel="icon" type="image/png" href={favicons.favicon192x192} sizes="192x192" />
    <link rel="apple-touch-icon" href={favicons.appleTouchIcon} />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} >
    <title>{pageTitle}</title>
    <style set:html={themeCSS} />
    <script is:inline>
      // Set theme immediately to prevent flash
      (function() {
        const stored = localStorage.getItem('theme');
        let theme = stored;
        if (!theme) {
          // Use system preference
          if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
            theme = 'dark';
          } else {
            theme = 'light';
          }
        }
        document.documentElement.setAttribute('data-theme', theme);
      })();
    </script>
  </head>
  <body>
    <Header />
    
    <main>
    <h1>{pageTitle}</h1>
    <slot />
    </main>
    
    <Footer />

  <script>
    import "../scripts/menu.js";
  </script>

  </body>
</html>
